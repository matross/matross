---
- configuration:
    application-name:
      connections:
        ssh:
          defaults:
            user: foo
          hosts:
            - user@localhost:1234
            - host: example.org
              user: foo
            - example.com
      vars:
        foo: "bar"
        baz: "buzz"

  application: "application-name"
  tasks:
    - name: "do x"
    - name: "do y"
    - name: "do z"
